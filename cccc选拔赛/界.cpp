#include <bits/stdc++.h>
using namespace std;
#define IOS                                                                    \
    ios::sync_with_stdio(false);                                               \
    cin.tie(nullptr);                                                          \
    cout.tie(nullptr);
#define endl "\n"

int n;

int main() {
    IOS;
    cin >> n;
    n--;
    while (n--) {
        cin >>
    }

    return 0;
}