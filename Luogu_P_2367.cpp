#include <iostream>
using namespace std;
#define ll long long

const int N = 5e6 + 10;
ll n, p;
ll nums[N];

int main() {
    cin >> n >> p;
    for (int i = 1; i <= n; i++) {
        cin >> nums[i];
    }

    while (p--) {
    }

    return 0;
}