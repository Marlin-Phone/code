#include <bits/stdc++.h>
using namespace std;
#define IOS                                                                    \
    ios::sync_with_stdio(0);                                                   \
    cin.tie(0);                                                                \
    cout.tie(0);
#define endl '\n'

const int N = 10010;
int n;
int nums[N] = {6, 2, 5, 5, 4, 5, 6, 3, 7, 6};
// =: 2 | +: 2
int arr[N];
int res = 0;

void dfs(int x) { // x 表示当前的位置

    for (int i = 1; i <= n; i++) {
    }
}
int main() {
    IOS;
    cin >> n;
    dfs(1);

    return 0;
}