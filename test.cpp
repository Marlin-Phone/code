#include <bits/stdc++.h>
using namespace std;

const int N = 1e5;
int head[N]; // 下标:节点号 数值:头边号
int next[N]; // 下标:边编号 数值:下一条边编号
int to[N];   // 下标:边编号 数值:去往的点
int cnt = 1; // 边的数量

int main() {
    //

    return 0;
}