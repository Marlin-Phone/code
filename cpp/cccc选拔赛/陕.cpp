#include <bits/stdc++.h>
using namespace std;
#define IOS                                                                    \
    ios::sync_with_stdio(false);                                               \
    cin.tie(nullptr);                                                          \
    cout.tie(nullptr);
#define endl "\n"

const int N = 50;
int n, m, T; // 食材种类, 菜肴种类, 工作时间
int b[N];    // 食材不新鲜的速率

int main() {
    IOS;
    cin >> n >> m >> T;
    for (int i = 1; i <= n; i++) {
        cin >> b[i];
    }
    while (m--) {
        cin >>
    }

    return 0;
}